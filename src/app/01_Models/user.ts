export interface User {
     user_id?: string; // ? optional
     firstname: string;
     lastname: string;
     phone: string;
     gender: string;
     email: string;
     photoURL: string;
     role_id: string;
 }